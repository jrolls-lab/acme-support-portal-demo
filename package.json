{
  "name": "acme-support-portal-demo",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx serve -l 3000",
    "start": "npx serve -l 3000",
    "build": "echo 'Static site — no build needed'"
  },
  "dependencies": {
    "serve": "^14.2.0"
  }
}
